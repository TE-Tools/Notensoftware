{
  "name": "notensoftware",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config worker/wrangler.toml",
    "test": "node --test tests/**/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
